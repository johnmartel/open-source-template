_extends: .github:_opensource.yaml

repository:
  name: open-source-template
  description: A basic template for creating new repositories
  topics: github, configuration, templates
  is_template: true
